import { Route } from '@angular/router';
import {CheckRecallPage} from './check-recall.page';

export const pagesCheckRecallRoutes: Route[] = [
   {path: '', pathMatch: 'full', component: CheckRecallPage}
];
