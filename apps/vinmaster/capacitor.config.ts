import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.test.vinmaster',
  appName: 'VINMaster',
  webDir: '../../dist/apps/vinmaster',
  bundledWebRuntime: false,
};

export default config;
